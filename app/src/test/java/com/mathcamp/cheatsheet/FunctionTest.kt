package com.mathcamp.cheatsheet

/**
 * Created by bkase on 7/13/16.
 */

// Doesn't work anymore :(
/*class FunctionTest: FreeSpec() {
  init {
    "Functions should compose" - {
      fun f(str: String): Int = str.length
      fun g(x: Int): Boolean = x > 10

      forAll { str: String ->
        val composed: (String) -> Boolean = f o g
        composed(str) == g(f(str))
      }
    }
  }
}*/
